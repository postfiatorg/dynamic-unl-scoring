fastapi>=0.115
httpx>=0.28
openai>=1.60
psycopg2-binary>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
structlog>=24.4
uvicorn[standard]>=0.30

# Scripts only (not needed by the service, install separately: pip install pyasn)
# pyasn>=1.6
